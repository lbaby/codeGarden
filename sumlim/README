可执行程序为sumlim
------------------------
-配置说明：
-----------------------
sumlim.inputPath=in
sumlim.filePattern=*.*
sumlim.tmpPath=tmp
sumlim.indexPath=index
sumlim.outputPath=out
sumlim.logPath=.
sumlim.errorPath=./err
sumlim.bakPath=bak
sumlim.backup=yes
sumlim.progID=00
最大内存占用（以M为单位）：
sumlim.maxmem=256
sumlim.sleepTime=3
每个结点的话单数，每天每SP的话单数，可以取平均数
sumlim.recordsPerNode=5000

===
== 取字段的配置 每个字段的取法和cut 的记法相同
比如： cut -c 1-1 
则在我们的配置字段里可以写作 1-1  
从8字节取到 9字节：8-9
取字段以后，各字段用 | 连接
比如：1-1|8-9 表示取1,8-9字节，连接在一起

用来建索引目录的字段
#svcName[0]+starDate(YYYYMM)
sumlim.dirPos=1-1|56-61

用来建索引文件名的字段
#startDate(DD) + spcode is file key
sumlim.filePos=62-63|139-158

用来作查找键值的字段
#msisdn and servcode
sumlim.keyPos=41-51|159-182

累加字段（费用)
#infofee: accumulation section
sumlim.valuePos=206-213

用以过滤话单的字段
#extract the chrgtype: 
sumlim.checkPos=204-205

过滤字段需满足是02才处理累加
#chrgtype we need  is 02
sumlim.checkKey=02

费率的最大封顶限制,以分为单位
sumlim.feeLimit=1500

===================================
以下字段加起来就是 msisdn spcode servcode 
chrgtype==02 startdate(YYYYMMDD) 为主键累加infoFee 
sumlim.dirPos
sumlim.filePos
sumlim.keyPos
sumlim.valuePos
sumlim.checkPos
sumlim.checkKey

=================================
错单格式：
已累计费用：话单
比如：
1501:D***
就是说，在累计完本条话单以后，费用是1501

==========================
附带的sumlim.cfg 即为正确的配置
